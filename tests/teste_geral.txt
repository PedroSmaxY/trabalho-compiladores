// Teste geral para o compilador P-
// Este arquivo testa as correcoes implementadas.

// 1. Teste de declaracoes sem chaves no programa principal
inteiro a, b, c;
real x;

// 2. Teste de precedencia de operadores
// A expressao deve ser avaliada como: a = (1 || (2 && 0))
a = 1 || 2 && 0;

// 3. Teste de ambiguidade (dangling else)
// O "senao" deve pertencer ao "se" mais interno (se b > 2).
b = 5;
c = 0;
se (b > 0) entao
    se (b > 2) entao
        c = 10;
    senao
        c = 20;

// 4. Teste de comandos gerais
x = 10.5;
enquanto (b > 0) {
    b = b - 1;
}

repita
    a = a + 1;
ate (a > 5);

ler(a, c);
mostrar(x / 2.0);

// Fim do programa de teste.