{
    inteiro a, b;
    ler(a, b);

    /* Teste se-entao-senao */
    se (a > b) entao {
        mostrar(a);
    } senao {
        mostrar(b);
    }

    /* Teste se-entao simples */
    se (a == b) entao
        mostrar(a);
}